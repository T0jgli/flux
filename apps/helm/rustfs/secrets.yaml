---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: rustfs-user
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: bitwarden-secretsmanager
    kind: ClusterSecretStore
  target:
    name: rustfs-user
  dataFrom:
    - extract:
        key: rustfs-rustfs-user
